<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.adjuva.backend.mapper.AutomationScheduleMapper">
    <select id="selectByProject" resultType="com.adjuva.backend.model.entity.AutomationSchedule">
        SELECT id, project_id, title, status, schedule_kind, schedule_expression,
               timezone, provider, model, conversation_title_template, prompt_template,
               last_triggered_at, metadata_json, created_at, updated_at
        FROM automation_schedules
        WHERE project_id = #{projectId}
        ORDER BY created_at DESC
    </select>

    <update id="markTriggered">
        UPDATE automation_schedules
        SET last_triggered_at = #{lastTriggeredAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>
</mapper>
