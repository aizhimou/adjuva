<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.adjuva.backend.mapper.ProviderSessionMapper">
    <select id="selectByConversationProviderModel" resultType="com.adjuva.backend.model.entity.ProviderSession">
        SELECT id, conversation_id, provider, model, external_session_id, status,
               metadata_json, last_seen_at, created_at, updated_at
        FROM provider_sessions
        WHERE conversation_id = #{conversationId}
          AND provider = #{provider}
          AND model = #{model}
    </select>
</mapper>
